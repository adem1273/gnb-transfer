# Render.com Production Environment Variables
# Copy these to your Render dashboard: Environment tab

# CRITICAL - Must be set
NODE_ENV=production
PORT=10000
CORS_ORIGINS=https://gnb-transfer.onrender.com
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
JWT_SECRET=[auto-generate in Render or use strong random string]

# Payment Integration - REQUIRED for payments
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Optional but Recommended
OPENAI_API_KEY=sk-your-openai-api-key-here
SENTRY_DSN=https://...@sentry.io/...
LOG_LEVEL=info
TRUST_PROXY=true

# Email Configuration (Optional - choose one)
# Gmail:
# EMAIL_PROVIDER=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-16-digit-app-password

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
STRICT_RATE_LIMIT_MAX=5

# Build Configuration (Already in render.yaml)
NPM_CONFIG_PRODUCTION=false
NODE_OPTIONS=--max-old-space-size=512
NPM_AUDIT=false

# Note: The render.yaml file already includes most of these.
# This file is for reference when setting up environment variables manually.
