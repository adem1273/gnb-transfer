# =============================================================================
# GNB Transfer Mobile App - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# For local development, use your machine's local IP address (not localhost)
# when running on physical device.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL - Required for all API calls
# For local development: http://YOUR_LOCAL_IP:5000/api (e.g., http://192.168.1.100:5000/api)
# For production builds: This is set in eas.json, not here
EXPO_PUBLIC_API_URL=http://localhost:5000/api

# -----------------------------------------------------------------------------
# Sentry Crash Reporting (FREE tier only)
# -----------------------------------------------------------------------------
# Get your DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
# Leave empty to disable Sentry completely.
#
# ⚠️ FREE TIER LIMITS:
# - Limited error events per month
# - Performance tracing is DISABLED in our config (paid feature)
# - Session replay is DISABLED (paid feature)
#
# Sentry is ONLY enabled in production builds (__DEV__ = false).
# In development, errors are logged to console only.
#
# SECURITY: Sentry integration is configured to:
# - Never send tokens, auth headers, or API payloads
# - Never send personal user data (PII)
# - Filter out HTTP breadcrumbs and console logs
# - Strip query parameters from navigation breadcrumbs
EXPO_PUBLIC_SENTRY_DSN=
