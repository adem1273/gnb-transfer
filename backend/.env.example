# Server Configuration
PORT=5000

# Database Configuration (MongoDB Atlas Free Tier)
# Example: mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Secret (use a strong random string - generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Bcrypt Salt Rounds (optional, defaults to 10)
BCRYPT_ROUNDS=10

# OpenAI API Key (optional, for AI features - REMOVED to reduce costs)
# OPENAI_API_KEY=

# OpenRoute API Key (optional, for routing features - use free tier)
# OPENROUTE_API_KEY=

# Stripe Secret Key (for payment processing - use test key for development)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# CORS Allowed Origins (comma-separated list)
# For production, set this to your frontend domain
# Example: https://your-frontend.vercel.app,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Node Environment
NODE_ENV=development

# Free-tier service recommendations:
# - MongoDB: Use MongoDB Atlas Free Tier (512MB storage)
# - Hosting: Use Render Free Tier or Railway Free Tier
# - Rate limits: Keep low to stay within free tier limits
