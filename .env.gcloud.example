# ============================================================================
# Google Cloud Environment Variables Template
# ============================================================================
# This file is a template for environment variables needed for Google Cloud deployment
# Copy this file to .env.gcloud and fill in your actual values
# Then use it with: gcloud run services update --env-vars-file=.env.gcloud
# ============================================================================

# IMPORTANT: DO NOT commit this file with real secrets to version control!
# Add .env.gcloud to .gitignore

# -----------------------------------------------------------------------------
# REQUIRED VARIABLES
# -----------------------------------------------------------------------------

# Application Environment
NODE_ENV=production

# Database Configuration
# Get from MongoDB Atlas or your MongoDB provider
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=

# JWT Secret for authentication
# Generate a strong random string (minimum 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=

# CORS Origins (comma-separated list of allowed origins)
# Include your frontend domain(s)
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# -----------------------------------------------------------------------------
# PAYMENT INTEGRATION
# -----------------------------------------------------------------------------

# Stripe Configuration
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# OPTIONAL INTEGRATIONS
# -----------------------------------------------------------------------------

# OpenAI API Key (for AI features)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Sentry Error Tracking
# Get from: https://sentry.io
SENTRY_DSN=

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION (Optional)
# -----------------------------------------------------------------------------

# Email Provider (gmail, smtp, sendgrid, etc.)
EMAIL_PROVIDER=

# Gmail Configuration
EMAIL_USER=
EMAIL_PASSWORD=

# Generic SMTP Configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

# -----------------------------------------------------------------------------
# REDIS CACHE (Optional)
# -----------------------------------------------------------------------------

# Redis Configuration (if using Redis for caching)
# Format: redis://username:password@host:port
REDIS_URL=

# -----------------------------------------------------------------------------
# CLOUDINARY (Optional)
# -----------------------------------------------------------------------------

# Cloudinary for image uploads
# Get from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# -----------------------------------------------------------------------------
# RATE LIMITING (Optional - defaults provided)
# -----------------------------------------------------------------------------

# Rate limit window in milliseconds (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX=100

# Strict rate limit for auth endpoints (default: 5)
STRICT_RATE_LIMIT_MAX=5

# -----------------------------------------------------------------------------
# LOGGING (Optional)
# -----------------------------------------------------------------------------

# Logging level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# TRUST PROXY (Optional)
# -----------------------------------------------------------------------------

# Trust proxy headers (automatically enabled in production)
# Values: true, false, or number of proxies
TRUST_PROXY=true

# -----------------------------------------------------------------------------
# FRONTEND BUILD-TIME VARIABLES
# -----------------------------------------------------------------------------
# Note: These are used during the build process and embedded in the frontend
# They should be set in Cloud Build or passed during docker build

# Frontend API URL
# This will be the URL of your Cloud Run service
# Example: https://gnb-transfer-xxxxx-uc.a.run.app/api
VITE_API_URL=

# Stripe Public Key
# Get from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLIC_KEY=

# Google Analytics 4 Measurement ID (optional)
VITE_GA_MEASUREMENT_ID=

# Sentry DSN for frontend error tracking (optional)
VITE_SENTRY_DSN=
